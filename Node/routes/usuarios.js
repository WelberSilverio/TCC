module.exports = function (app) {
	app.get('/usuarios/login', function (req, res) {
		// var user = req.body;
		// var connection = app.models.connectionFactory();
		// var UsuarioConsulta = new app.models.usuarios(connection);

		// UsuarioConsulta.login(user, function (err, result) {
		// 	//ação
		// })
	});

};


